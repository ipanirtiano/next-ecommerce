import FormSignIn from "./FormSign";

const page = () => {
  return(
    <div className="w-full bg-gray-100 flex items-center justify-center h-screen">
      <FormSignIn />
    </div>
  )
};

export default page;
